import React from 'react';
import { PredictionWorkspace } from '../components/PredictionWorkspace';

export const Predict: React.FC = () => <PredictionWorkspace initialTab="live" />;

export default Predict;
