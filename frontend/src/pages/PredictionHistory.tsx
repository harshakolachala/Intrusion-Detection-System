import React from 'react';
import { PredictionWorkspace } from '../components/PredictionWorkspace';

export const PredictionHistory: React.FC = () => <PredictionWorkspace initialTab="history" />;

export default PredictionHistory;
